## Server (Colyseus)
## Copy to `server/.env` for local development.

# Server port (default: 2567)
PORT=2567

# Use "production" on Dokploy
NODE_ENV=development

# Optional: serve built client from this server (set true if you want a single deployable)
SERVE_CLIENT=false

# Optional: allow embedding the game in iframes.
# Example: FRAME_ANCESTORS='self' https://jelibonsoft.com https://www.jelibonsoft.com
FRAME_ANCESTORS=

# Optional: enable Colyseus monitor panel at /colyseus
MONITOR_ENABLED=false
MONITOR_USER=
MONITOR_PASS=
