{
	"name": "client",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite --host",
		"preview": "vite preview",
		"zip": "npm run build && node -e \"const { execSync } = require('node:child_process'); execSync('zip -r game.zip . -x \\\\\\\"**/.DS_Store\\\\\\\"', { cwd: 'dist', stdio: 'inherit' });\""
	},
	"dependencies": {
		"colyseus.js": "^0.16.22",
		"kaplay": "3001.0.19"
	},
	"devDependencies": {
		"vite": "^5.4.10"
	}
}
