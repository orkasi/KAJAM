{
	"name": "client",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite --host",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\""
	},
	"dependencies": {
		"colyseus.js": "^0.15.26",
		"kaplay": "3001.0.2"
	},
	"devDependencies": {
		"vite": "^5.4.10"
	}
}
